# データ同期の一方通行化タスク

## 目的

管理画面（admin）から表示画面（display）へのデータ同期を一方通行にし、ループや競合を防ぐ。

## タスク

- [x] 実装計画の作成
- [x] `setupBroadcastChannel`の実装見直し
  - 管理画面（isAdminMode=true）では受信しない
  - 表示画面（isDisplayMode=true）では送信しない
- [ ] 動作確認
  - 管理画面で入力 -> 表示画面に反映
  - 管理画面のリロードでデータが消えないか確認
